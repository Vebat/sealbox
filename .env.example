# Generate both with: openssl rand -base64 32
# Losing the master key loses every value stored in sealbox. Back it up outside the database.
SEALBOX_MASTER_KEY=
# Bearer token clients must send. Rotate by restarting with a new value.
SEALBOX_API_KEY=
